<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rprescott.dndtool.server.database.login.LoginDAO">
  
  <select id="getUserCredentialByName" resultType="CredentialDTO">
    SELECT
        user_id AS id,
        username,
        salt,
        hash
    FROM "user"
    WHERE username = #{userName}
  </select>
  
  <insert id="registerNewUser" useGeneratedKeys="true" keyColumn="user_id" keyProperty="id" parameterType="CredentialDTO">
    INSERT INTO "user" (
        username,
        salt,
        hash
    )
    VALUES (
        #{userName},
        #{salt},
        #{hash}
    )
  </insert>
  
</mapper>